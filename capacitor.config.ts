import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'colmado-angular-frontend',
  webDir: 'www'
};

export default config;
